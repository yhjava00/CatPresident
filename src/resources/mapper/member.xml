<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="member">
  
 	<select id="check_id" parameterType="String" resultType="String">
 		select id from member where id= #{id}
 	</select>
 	
 	<select id="login" parameterType="String" resultType="String">
 		select pw from member where ID = #{member_id}
 	</select>

 	<select id="getMember" parameterType="String" resultType="memberVO">
 		select * from member where id = #{id}
 	</select>
 	
 	<insert id="insertMember" parameterType="memberVO">
 		insert into member(id,pw,name,phone,joindate) values(#{id},#{pw},#{name},#{phone},sysdate)
 	</insert>
 	
 	<select id="selectMember" parameterType="String" resultType="memberVO">
 		select id, name, phone, joindate from member where id = #{id}
 	</select>
 	
</mapper>
